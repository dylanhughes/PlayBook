---
- name: Install httpd on RedHat
  hosts: gb3wwlaweb03
  connection: ssh
    tasks:
    - name: yum update
      action: yum name=* state=latest
    - name: install httpd
      yum: pkg=httpd state=latest
